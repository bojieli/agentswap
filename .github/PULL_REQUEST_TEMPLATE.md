## What this changes

<!-- What is different for someone using agentswap, in a sentence or two. -->

## Why

<!-- The failure this fixes, or the situation that made it worth doing.
     If it fixes an issue, link it. -->

## Checklist

- [ ] `make check` passes (build, vet, gofmt, no dependencies, race tests)
- [ ] A test fails without this change, if it fixes a bug
- [ ] `go.sum` is still empty
- [ ] No credential can reach a log line
- [ ] Comments explain *why*, where the reasoning is not obvious from the code
